# Copy this file to backend/.env and fill in real values locally.
# Never commit real API keys.

# Default model provider: vei, ark, or openai
LLM_PROVIDER=ark

# Volcengine VEI AI Gateway / Doubao, OpenAI-compatible chat endpoint
VEI_API_KEY=replace_me
VEI_BASE_URL=https://ai-gateway.vei.volces.com/v1
VEI_MODEL=doubao-1.5-lite-32k

# Volcengine Ark / Doubao, OpenAI-compatible chat endpoint
ARK_API_KEY=replace_me
ARK_OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_MODEL=doubao-seed-2-1-pro-260628

# OpenAI official API
OPENAI_API_KEY=replace_me
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini

# Speech-to-text. Use volc_seedasr for Doubao Voice file recognition.
STT_PROVIDER=volc_seedasr
STT_MODEL=whisper-1
OPENAI_STT_MODEL=whisper-1
ARK_STT_MODEL=
VEI_STT_MODEL=
VOLC_ASR_API_KEY=replace_me
VOLC_ASR_RESOURCE_ID=volc.seedasr.auc
VOLC_ASR_MODEL=bigmodel
VOLC_ASR_SUBMIT_URL=https://openspeech.bytedance.com/api/v3/auc/bigmodel/submit
VOLC_ASR_QUERY_URL=https://openspeech.bytedance.com/api/v3/auc/bigmodel/query
VOLC_ASR_POLL_INTERVAL_SEC=1.0
VOLC_ASR_MAX_POLLS=20

# Optional tracing variables. This backend does not require LangChain.
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=replace_me
LANGSMITH_PROJECT=smartcane
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=replace_me
LANGCHAIN_PROJECT=smartcane

# Amap / Gaode. Keep real keys in backend/.env and frontend/SmartCane/local.properties only.
AMAP_WEB_KEY=replace_me
AMAP_ANDROID_KEY=replace_me

# Optional local speech-to-text fallback. Install backend requirements including faster-whisper,
# then set STT_PROVIDER=local to avoid relying on phone/system speech services or cloud STT.
# LOCAL_STT_MODEL=base
# LOCAL_STT_DEVICE=cpu
# LOCAL_STT_COMPUTE_TYPE=int8
